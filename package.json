{
  "name": "fitvault-fixed",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "deploy:ci": "npx wrangler versions upload",
    "check": "node --check public/app.js && node --check worker/index.js"
  },
  "devDependencies": {
    "wrangler": "^4.13.2"
  }
}
